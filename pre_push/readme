pre-push
pre-push 훅이 받는 인자
 destination remote's name, destination remote's location
왜 push하는 커밋이 여러개일 때도 stdin으로 받는 정보가 여러 줄이 아닐까?
 local-oid와 remote-oid를 범위로 그 사이의 커밋을 가져올 수 있다
  local-oid가 더 최신 커밋 해시이고, remote-oid는 더 이전 커밋 해시다.
 여러 remote에 한 번에 push할 경우 각 remote마다 한 줄씩인 건가?
